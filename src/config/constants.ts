// Đường dẫn trỏ về Backend Spring Boot của chúng ta
export const BASE_URL = import.meta.env.VITE_API_BASE_URL;

// Các hằng số khác dùng chung cho toàn dự án
export const STORAGE_KEYS = {
  TOKEN: 'access_token',
  USER_INFO: 'user_info',
};